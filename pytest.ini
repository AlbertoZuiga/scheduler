[pytest]
# `tests/` no es un paquete, asi que pytest inserta ese directorio en sys.path,
# no la raiz del repo: sin esto `import app` falla al invocar `pytest` directo.
pythonpath = .
testpaths = tests
